@model CoreCodedChatbot.ApiContract.ResponseModels.Playlist.ChildModels.PlaylistItem[]

<div class="request even">
    <div class="requestHeader"><b class="vipText">VIP</b><b> Song Requests</b></div>
</div>

@for (var i = 0; i < Model.Length; i++)
{
    Html.RenderPartial("Partials/List/PlaylistItem", Model[i]);
}